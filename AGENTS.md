# AGENTS.md

## 기술 스택

- Python 3.12 / FastAPI / PostgreSQL
- Redis / Celery

## 문서

기능을 구현하거나 수정하기 전에 아래 문서를 먼저 읽습니다.
- `docs/PROGRESS.md`: 현재 작업 진행 상태를 확인합니다.
- `docs/prd/`: 구현 대상 기능의 PRD만 읽습니다. 관련 없는 PRD는 읽지 않습니다.
  - 구현은 지정된 PRD를 최우선 기준으로 합니다.
  - 여러 PRD가 필요한 경우에만 추가로 읽습니다.
  - 구현 대상 PRD가 명시되지 않은 경우에는 먼저 사용자에게 확인을 요청합니다.

## 작업 원칙

- 요청받지 않은 리팩토링은 수행하지 않습니다.
- 요청 범위를 벗어난 파일은 수정하지 않습니다.
- 기존 코드 스타일과 프로젝트 구조를 우선합니다.
- 큰 구조 변경이 필요한 경우, 먼저 변경 계획을 제시합니다.

## 기능 구현 규칙

- PRD 내용과 사용자의 채팅 지시가 상충되면, 사용자에게 우선순위를 확인합니다.
- PRD에 정의되지 않은 엣지 케이스나 예외 상황을 발견하면, 이를 사용자에게 알리고 PRD 업데이트를 제안합니다.
- 기능 구현이 완료되면, `docs/PROGRESS.md`를 갱신합니다.

## FastAPI

- 라우터는 요청/응답 처리와 의존성 조립에 집중하고, 비즈니스 로직은 별도의 서비스 계층에 둡니다.
- FastAPI 의존성 주입은 `Depends()`를 직접 파라미터 기본값으로 작성하기보다 `Annotated`를 우선 사용합니다.
- 반복해서 사용하는 의존성은 타입 별칭으로 정의하여 재사용합니다.
- 의존성 함수와 서비스의 입력/출력에는 타입 힌트를 명시합니다.
- 요청 본문, 응답 모델, 설정 및 데이터 검증에는 Pydantic 모델을 사용합니다.
- API 응답 스키마와 ORM 모델을 불필요하게 결합하지 않습니다.
- `response_model` 또는 반환 타입을 명시하여 API의 입출력 타입이 드러나도록 작성합니다.
- 동기 I/O와 비동기 I/O를 혼용할 때 실행 모델을 명확히 구분합니다.
- `async def` 내부에서 블로킹 I/O를 직접 실행하지 않습니다.

## 타입 힌트

- 함수와 메서드의 파라미터 및 반환값에 타입 힌트를 작성합니다.
- 클래스 속성에도 가능한 경우 타입을 명시합니다.
- `Any` 사용은 필요한 경우로 제한합니다.
- `dict`, `list`, `tuple` 등은 Python 3.12 기본 제네릭 문법을 사용합니다.
- FastAPI의 DI처럼 타입 정보와 메타데이터를 함께 표현해야 하는 경우 `typing.Annotated`를 우선 사용합니다.

## 데이터베이스

- 데이터베이스 접근은 SQLAlchemy 2를 사용합니다.
- DB 세션은 FastAPI 의존성 주입을 통해 전달합니다.
- 세션이나 커넥션을 전역 상태로 직접 공유하지 않습니다.
- 트랜잭션 경계가 필요한 경우 명시적으로 관리합니다.
- 라우터에서 직접 복잡한 쿼리나 영속성 로직을 작성하지 않습니다.
- 기존 Repository 패턴이 사용 중이면 해당 구조를 따릅니다. 새로운 Repository 계층은 필요성이 명확할 때만 추가합니다.

## 테스트

- 테스트는 `pytest`를 사용하여 작성합니다.
- 새로운 기능에는 최소한의 단위 테스트를 함께 작성합니다.
- 기능 구현 또는 수정 후 관련 테스트를 실행합니다.
- 작업 완료 전 전체 테스트를 실행합니다. `uv run pytest`

## 스코프

- 수정 금지: `.env`
- 읽기 금지: `.env`

## 코드 스타일

- Python 코드는 `ruff format`으로 포맷팅합니다.
- import 문은 절대 경로를 사용하며, Ruff(isort)를 이용하여 정렬합니다.
- 공개 클래스와 함수에는 Google 스타일 docstring을 작성합니다.
- docstring 본문 설명은 한국어로 작성하되, Google 스타일 섹션 헤더는 `Args:`, `Returns:`, `Raises:`처럼 영어로 작성합니다.
- 타입 힌트를 작성합니다.

## 실행

- Python 패키지 추가/제거는 `uv add`/`uv remove`를 사용합니다.
- Python 파일 실행 시 `uv run python` 명령어를 사용합니다.
- 